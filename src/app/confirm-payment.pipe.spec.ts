import { ConfirmPaymentPipe } from './confirm-payment.pipe';

describe('ConfirmPaymentPipe', () => {
  it('create an instance', () => {
    const pipe = new ConfirmPaymentPipe();
    expect(pipe).toBeTruthy();
  });
});
